import { MesesTextoPipe } from './meses-texto.pipe';

describe('MesesTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new MesesTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
